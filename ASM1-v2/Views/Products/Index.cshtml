@model ASM1_v2.Models.productcategoryviewmodels

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<style>
    form select{
        margin-bottom: 5px;
    }
</style>
<form asp-controller="Products" asp-action="Index" method="get" style = "display: flex;
    flex-direction: row;
    justify-content: space-around; 
margin-bottom: 15px;">
<div>categoryID: 
    <select asp-for="ProCategory" asp-items="Model.Category">
        <option value="">All</option>
    </select></div>
<div> Price:
    <select asp-for="ProPrice" asp-items="Model.Price">
        <option value="">All</option>
    </select></div>
<div>Title:
    <input type="text" asp-for="SearchString" /></div>

    
   
    
    <input type="submit" value="Filter" />
    <a asp-action="Index">Back to Full List</a>
</form>

<div class = "container-fluid">
    <div class = "panel panel-primary">
        <div class  = "panel-heading">Products</div>
        <div class = "panel-body">
            <table class="table table-bordered table-sm">
    <thead>
        <tr>
             <th>
                @Html.DisplayNameFor(model => model.Products[0].Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products[0].Unit)
            </th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in  Model.Products) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Unit)
            </td>
            <td> <a asp-action="Edit" asp-route-id="@item.ID" class = "btn btn-primary">Edit</a>
               </td>
            <td> <a asp-action="Details" asp-route-id="@item.ID" class = "btn btn-primary">Details</a>
               </td>
               <td> <a asp-action="Delete" asp-route-id="@item.ID" class = "btn  btn-primary">Delete</a></td>
        </tr>
}
    </tbody>
</table>

        </div>
    </div>
</div>
